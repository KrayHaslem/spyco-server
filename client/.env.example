# API base URL (must include /api path).
# Defaults to /api (Vite dev proxy) when unset.
# For direct API: http://127.0.0.1:5000/api
# For production: https://api.example.com/api
# VITE_API_BASE_URL=
